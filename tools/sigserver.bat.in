@if NOT DEFINED SIGVERSE_ROOT @set PATH=${JRE_ROOT_DIR}\bin\client;%PATH%

@set SIGVERSE_ROOT=${CMAKE_INSTALL_PREFIX}\

@set SIGVERSE_X3DPARSER_CONFIG=%SIGVERSE_ROOT%share\sigverse\etc\X3DParser.cfg
@set SIGVERSE_RUNAC=%SIGVERSE_ROOT%bin\sigrun.exe
@set SIGVERSE_DATADIR=%SIGVERSE_ROOT%share\sigverse\data

%SIGVERSE_ROOT%bin\simserver.exe %*
